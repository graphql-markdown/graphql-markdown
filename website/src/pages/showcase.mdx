import NpmDownloads from "@site/src/components/NpmDownloads";
import showcaseData from "@site/src/data/showcase.json";

<div>
  <section class="margin-top--lg margin-bottom--lg text--center">
    <h1>GraphQL-Markdown Showcase</h1>
    <p>List of documentation websites built with GraphQL-Markdown</p>
    <p className="margin-bottom--md npm-downloads__caption">
      <NpmDownloads pkg="@graphql-markdown/core" href="https://www.npmjs.com/package/@graphql-markdown/core" />
    </p>
  </section>

  <div className="showcase">
    <div className="showcase-section-label">Featured</div>
    <div className="showcase-featured">
      {showcaseData.filter(s => s.featured).map(s => (
        <ShowcaseCard key={s.name} featured name={s.name} href={s.href} image={s.image} />
      ))}
    </div>
    <hr className="showcase-divider" />
    <div className="showcase-section-label">All sites</div>
    <div className="showcase-all">
      {[...showcaseData].sort((a, b) => a.name.localeCompare(b.name)).map(s => (
        <ShowcaseCard key={s.name} name={s.name} href={s.href} image={s.image} />
      ))}
      <a
        href="https://github.com/graphql-markdown/graphql-markdown/issues/1601"
        target="_blank"
        rel="noopener noreferrer"
        className="showcase__add-card"
      >
        <span className="showcase__add-icon">+</span>
        <span className="showcase__add-text">Add your site</span>
      </a>
    </div>
  </div>
</div>
