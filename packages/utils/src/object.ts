/**
 * Object functions
 */

// export function hasProperty(obj, prop) {
//   return (
//     !!(obj && obj[prop]) ||
//     (obj instanceof Object && Object.prototype.hasOwnProperty.call(obj, prop))
//   );
// }

// export function hasMethod(obj, prop) {
//   return hasProperty(obj, prop) && typeof obj[prop] === "function";
// }
