const {
  checkSchemaChanges,
  saveSchemaHash,
  saveSchemaFile,
} = require("@/lib/diff");

describe("lib", () => {
  describe("diff", () => {
    describe("checkSchemaChanges()", () => {
      test("does something", () => {
        expect(true).toBeTruthy();
      });
    });
    describe("saveSchemaHash()", () => {
      test("does something", () => {
        expect(true).toBeTruthy();
      });
    });
    describe("saveSchemaFile()", () => {
      test("does something", () => {
        expect(true).toBeTruthy();
      });
    });
  });
});
