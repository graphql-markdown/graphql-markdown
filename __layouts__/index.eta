<% if (['Query','Mutation','Subscription'].includes(it.kind)) { 
  layout('./operation', {...it, operation: it.kind.toLowerCase() })
} else if (it.kind === "ScalarTypeDefinition") {
  layout('./scalar', it)
} else {
  layout('./object', it)
} %>
